# Frontend Environment Variables

# API Base URL - defaults to /api/v1 which proxies through Vite in development
# For production, set to the full backend URL (e.g., https://api.example.com/api/v1)
VITE_API_BASE_URL=/api/v1
